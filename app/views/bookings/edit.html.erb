<% if logged_in? %>
  <p>Logged in, Hi <%= current_user.name %></p>
  <%= link_to "Log out", logout_path, method: :delete %>
  <%= render partial: "form" %>
<% else %>
  <p>Please <%= link_to "Log in", login_path%> first</P>
<% end %>
